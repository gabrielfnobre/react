import { StyleSheet } from "react-native";

export const estilo = StyleSheet.create({

    centralizaTudoLayout: {
        alignItems: 'center',
        flex: 1,
        justifyContent: 'center',
    }

})

export default estilo